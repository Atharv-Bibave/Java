/*
method: public boolean startsWith(String prefix, int offset);

description:
	-predict which determines if the given String contains the gives prefix beginning comparison at toffset.
	-The result is false if the 
